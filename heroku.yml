setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    release:
      dockerfile: Dockerfile
      target: production
    web: Dockerfile
    worker: .
release:
  image: worker
  command:
    - npm run typeorm:run