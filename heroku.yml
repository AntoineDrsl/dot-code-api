setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    release:
      dockerfile: Dockerfile
      target: production
    web: Dockerfile
    worker: ./Dockerfile
release:
  image: worker
  command:
    - npm run typeorm:run