setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    release:
      dockerfile: ./Dockerfile
      target: production
      command:
        - npm run typeorm:run
    web: Dockerfile
# release:
#   image: worker
#   command:
#     - npm run typeorm:run